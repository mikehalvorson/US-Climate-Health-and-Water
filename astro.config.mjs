// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://mikehalvorson.github.io',
  base: '/US-Climate-Health-and-Water/',
  output: 'static',
  trailingSlash: 'ignore',
});
